# fork-exec-bash-repro

Reproduction harness for intermittent CircleCI errors:

```
failed to start cmd: fork/exec /bin/bash: operation not permitted
```

Tests multiple Docker image and executor configurations on Cloud Docker.
